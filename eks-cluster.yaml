apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: smartops-cluster
  region: us-east-1

managedNodeGroups:
  - name: smartops-nodes
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
    privateNetworking: true